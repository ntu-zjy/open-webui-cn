# Dependabot disabled in this fork — upstream config targets the `dev` branch
# which does not exist here.  Keep the file present so GitHub does not
# auto-enable the default security updates.
version: 2
updates: []
